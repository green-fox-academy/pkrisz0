import java.util.ArrayList;

public class Deck extends ArrayList<Card> {
/*

    public Deck(int size) {
        for (int i = 0; i < ; i++) {
            this.add(new Card());
        }

    }
    */
}
// Create a constructor that takes a whole number as parameter
// The constructor should fill the list with the number of different
// cards using at least 4 different colors (except if the number given
// is smaller than four, than all cards should have different colors)

